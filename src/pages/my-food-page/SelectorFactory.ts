import { createSelector } from 'reselect'
import * as EventFactory from 'page.my-food-page/redux/event-factory'

const selectors = [
    
]

export default dispatch => createSelector(selectors, (...args) => {

    const [

    ] = args

    return {
    }
})